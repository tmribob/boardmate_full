[pytest]
pythonpath = .
testpaths = app/tests
markers =
    unit: fast isolated tests for pure logic
    integration: api and module integration tests
    e2e: end-to-end business flow tests with mocked boundaries
